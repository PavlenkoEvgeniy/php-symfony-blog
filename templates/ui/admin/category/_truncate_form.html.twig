<form method="post" action="{{ path('ui_admin_category_truncate', {'id': category.id}) }}" onsubmit="return confirm('Вы уверены что хотите удалить насовсем?');">
    <input type="hidden" name="_token" value="{{ csrf_token('truncate' ~ category.id) }}">
    <input type="hidden" name="_method" value="delete">
    <button class="btn btn-danger">Уничтожить</button>
</form>
